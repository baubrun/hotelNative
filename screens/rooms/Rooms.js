import React from 'react';
import {StyleSheet, FlatList, View, Text} from 'react-native';

const Room = props => {
  return (
    <View>
      <Text>rooms</Text>
    </View>
  );
};

export default Room;
